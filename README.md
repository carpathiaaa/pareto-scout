# pareto-scout
A three-agent system that finds and surfaces leads, subject matter experts, and job seekers from free public sources. Each agent takes a plain-language search query, parses it into structured criteria, fetches candidates, scores them for relevance using an LLM, and drafts a personalized outreach message
