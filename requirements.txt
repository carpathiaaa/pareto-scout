# Python 3.11+
# Runtime
httpx==0.27.2            # all data-source HTTP (Apollo, Hunter, SerpAPI); not requests
openai==1.54.4           # OpenAI-compatible client, pointed at Groq via base_url
supabase==2.9.1          # supabase-py, configured for the agency schema
python-dotenv==1.0.1     # .env loading via shared/utils.get_env

# Dev / quality (per CLAUDE.md /lint and /test)
black==24.10.0
flake8==7.1.1
pytest==8.3.3
