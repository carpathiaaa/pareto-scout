# Secrets — never commit. Only .env.example is tracked.
.env
.env.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/

# Node / Next.js (web/ lands later)
node_modules/
.next/
web/.env*.local

# OS / editor
.DS_Store
Thumbs.db

# Keep skills tracked (.agents/ + skills-lock.json are committed on purpose).
# .claude/skills/ holds machine-specific symlinks, so ignore it.
.claude/skills/
