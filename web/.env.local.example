# Copy to web/.env.local and fill in. Only NEXT_PUBLIC_ vars reach the browser.
# These are the SAME Supabase project as the Python agents, but the ANON key here
# (not the service-role key, which must never reach the browser).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
